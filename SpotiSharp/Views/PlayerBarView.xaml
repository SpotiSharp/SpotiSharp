<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SpotiSharp.Views.PlayerBarView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image Grid.Row="0" Grid.Column="0" Grid.RowSpan="3"
                   Source="{Binding SongImageURL}"
                   HeightRequest="60" />
            <Label Grid.Row="1" Grid.Column="1" Text="{Binding SongName}" FontSize="18" Margin="20,0,0,0" VerticalOptions="Center" />
        </Grid>
        <Grid Grid.Row="0" Grid.Column="1" VerticalOptions="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Row="0" Grid.Column="0" Text="Shuffle" Command="{Binding ChangeShuffle}" Margin="0,0,5,0"/>
            <Button Grid.Row="0" Grid.Column="1" Text="Back" Command="{Binding SongBack}" Margin="5,0,5,0"/>
            <Button Grid.Row="0" Grid.Column="2" Text="Play/Pause" Command="{Binding TogglePlaying}" Margin="5,0,5,0"/>
            <Button Grid.Row="0" Grid.Column="3" Text="Skip" Command="{Binding SongSkip}" Margin="5,0,5,0"/>
            <Button Grid.Row="0" Grid.Column="4" Text="Repeat" Command="{Binding ChangeRepeat}" Margin="5,0,0,0"/>
        </Grid>
        <Frame Grid.Row="0" Grid.Column="2" BackgroundColor="Transparent" BorderColor="Transparent" />
        <Slider Grid.Row="0" Grid.Column="3"
                WidthRequest="200"
                Minimum="0"
                Maximum="100"
                Value="{Binding SelectedVolume}"
                Margin="0,0,20,0"
                VerticalOptions="Center" />
    </Grid>
</ContentView>