<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:inputs="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
             x:Class="SpotiSharp.Views.Filters.PlaylistNumberFilterView">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding FilterName}"/>
            <Entry Grid.Row="1" Grid.Column="0" Keyboard="Numeric" Text="{Binding EnteredNumber}"/>
            <inputs:SfComboBox Grid.Row="1" Grid.Column="1" WidthRequest="200" IsEditable="False" ItemsSource="{Binding FilterOptions}" SelectedItem="{Binding SelectedFilterOption}"/>
            <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Text="Remove" Command="{Binding RemoveFilterCommand}"
                    BackgroundColor="Red" TextColor="Black"/>
        </Grid>
</ContentView>